# 모델 1차 개선안

> 아크 검출 CNN 프로젝트 — Kaggle 본 학습 중 확인된 **긴 학습 시간, CPU 병목, 네트워크 중단 위험**을 줄이기 위한 1차 개선안입니다.

---

## 1. 최종 판단

팀원이 가져온 병합본을 검토한 결과, 우리 프로젝트의 1차 개선안으로 가장 적합한 방식은 아래 조합입니다.

```text
RAM preload backend + h5 backend 유지 + epoch/latest checkpoint + resume
```

선택 이유:

- Kaggle 사용 가능 RAM이 약 `31.9GB`로 확인되어 train/valid를 `int16`으로 RAM에 올릴 수 있음
- 기존 h5 방식은 매 epoch마다 h5를 다시 읽어 CPU/I/O 병목이 큼
- RAM backend는 데이터 내용을 바꾸지 않고 공급 방식만 바꾸므로 baseline 비교가 가능함
- `h5` backend를 기본값으로 남겨 기존 실험과 누수 검증을 유지할 수 있음
- epoch별 checkpoint와 `latest.keras` 저장으로 중단 후 이어학습 가능

TFRecord 방식은 장기적으로 좋은 선택이지만, 지금 단계에서는 변환 스크립트, 저장 전략, 검증 비용이 추가로 필요합니다. 따라서 **TFRecord는 2차 개선안으로 보류**합니다.

---

## 2. 이번 개선에서 바뀐 파일

변경 파일:

```text
config.py
data.py
train.py
.gitignore
```

건드리지 않은 파일:

```text
model.py
evaluate.py
```

즉 모델 구조와 평가 지표는 유지하고, **데이터 공급 방식과 학습 중단 복구 방식만 개선**했습니다.

---

## 3. 핵심 개선 1 — RAM backend 추가

기존 방식:

```text
h5 파일을 batch마다 읽음
epoch마다 h5를 다시 순회
CPU/I/O 병목 발생 가능
```

개선 방식:

```text
train/valid split을 int16으로 RAM에 한 번 preload
batch 디코드, transpose, 정규화는 tf.data.map에서 병렬 처리
epoch마다 디스크를 다시 읽지 않음
```

추가된 주요 함수:

```python
preload_split_int16()
compute_ram_channel_stats()
make_ram_tf_dataset()
```

RAM backend 사용 시:

```bash
python train.py \
  --h5-path /kaggle/input/arc-fault-dataset/arc_fault_dataset.h5 \
  --channels all \
  --split-mode author \
  --normalize global \
  --epochs 20 \
  --batch-size 256 \
  --run-name v1 \
  --data-backend ram
```

기존 h5 방식도 그대로 사용 가능합니다.

```bash
python train.py \
  --h5-path /kaggle/input/arc-fault-dataset/arc_fault_dataset.h5 \
  --channels all \
  --split-mode author \
  --normalize global \
  --epochs 20 \
  --batch-size 256 \
  --run-name v1 \
  --data-backend h5
```

---

## 4. 핵심 개선 2 — Checkpoint / Resume

학교 Wi-Fi 또는 Kaggle 세션 문제로 학습이 끊겨도 이어받을 수 있도록 저장 방식을 바꿨습니다.

저장되는 파일:

```text
models/all_author_global_v1.keras
models/checkpoints/all_author_global_v1/epoch_001.keras
models/checkpoints/all_author_global_v1/epoch_002.keras
models/checkpoints/all_author_global_v1/latest.keras
results/all_author_global_v1_history.csv
```

역할:

| 파일 | 역할 |
|---|---|
| `models/{run_id}.keras` | val_loss 기준 최고 모델 |
| `models/checkpoints/{run_id}/epoch_XXX.keras` | 각 epoch 완료 시점 모델 |
| `models/checkpoints/{run_id}/latest.keras` | 가장 최근 epoch 모델, 이어학습용 |
| `results/{run_id}_history.csv` | 학습 로그 |

중단 후 이어서 학습:

```bash
python train.py \
  --h5-path /kaggle/input/arc-fault-dataset/arc_fault_dataset.h5 \
  --data-backend ram \
  --channels all \
  --split-mode author \
  --normalize global \
  --epochs 20 \
  --batch-size 256 \
  --run-name v1 \
  --resume-from models/checkpoints/all_author_global_v1/latest.keras \
  --initial-epoch 7
```

`--initial-epoch 7`은 epoch 7까지 완료됐고 다음 학습을 epoch 8부터 시작한다는 의미입니다.

---

## 5. 핵심 개선 3 — 기존 h5 backend 보존

기본값은 여전히 `h5`입니다.

```bash
--data-backend h5
```

따라서 아무 옵션을 주지 않으면 기존 코드 흐름과 거의 동일하게 동작합니다.

RAM backend v1의 제약:

```text
split_mode="author"만 지원
```

이유:

- RAM backend의 1차 목표는 본 학습 속도 개선
- `random`/`block` 누수 검증은 자주 돌리지 않으므로 기존 h5 backend로 유지
- 기능을 한 번에 넓히기보다 안정적인 author 학습부터 개선

---

## 6. 검토 결과

이번 병합본은 우리 프로젝트에 적합합니다.

좋은 점:

- 모델 구조를 바꾸지 않음
- 평가 코드를 바꾸지 않음
- 기존 h5 방식이 유지됨
- RAM 방식과 h5 방식의 수치 동일성 검증 결과가 있음
- 중단 후 이어학습 경로가 명확함
- Kaggle RAM 31.9GB 조건에서 실현 가능성이 높음

주의할 점:

- RAM backend는 train+valid를 RAM에 올리므로 RAM이 낮은 환경에서는 실패할 수 있음
- `--ram-limit`는 OOM 대비 또는 빠른 테스트용이며, 공식 성능 기록에는 전체 데이터(`--ram-limit` 미사용)를 권장
- 긴 학습은 Kaggle의 **Save & Run All**로 실행하고, 실패 시 `latest.keras`에서 resume하는 운영이 가장 안전함

---

## 7. 운영 권장 방식

1. 짧은 확인:

```bash
python train.py \
  --h5-path /kaggle/input/arc-fault-dataset/arc_fault_dataset.h5 \
  --data-backend ram \
  --channels all \
  --split-mode author \
  --normalize global \
  --epochs 2 \
  --batch-size 256 \
  --run-name ram_smoke
```

2. 본 학습:

```bash
python train.py \
  --h5-path /kaggle/input/arc-fault-dataset/arc_fault_dataset.h5 \
  --data-backend ram \
  --channels all \
  --split-mode author \
  --normalize global \
  --epochs 20 \
  --batch-size 256 \
  --run-name v1
```

3. 중단 후 이어학습:

```bash
python train.py \
  --h5-path /kaggle/input/arc-fault-dataset/arc_fault_dataset.h5 \
  --data-backend ram \
  --channels all \
  --split-mode author \
  --normalize global \
  --epochs 20 \
  --batch-size 256 \
  --run-name v1 \
  --resume-from models/checkpoints/all_author_global_v1/latest.keras \
  --initial-epoch 완료한_epoch_번호
```

---

## 8. 결론

우리 팀의 공식 1차 개선안은 아래로 확정하는 것이 좋습니다.

```text
속도 개선: RAM preload backend
중단 복구: epoch/latest checkpoint + resume
운영 방식: Kaggle Save & Run All + 실패 시 latest.keras resume
장기 개선: TFRecord는 2차 개선안으로 보류
```

이번 개선은 ESP 이식 방향과도 충돌하지 않습니다. RAM/h5/TFRecord는 학습 데이터 공급 방식이고, 최종 ESP 이식은 학습된 `.keras` 모델을 TFLite/int8로 변환하는 별도 단계입니다.
